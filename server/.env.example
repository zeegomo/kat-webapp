# KAT CouchDB Server Configuration
# Copy this file to .env and fill in the values
#
# cp .env.example .env
# chmod 600 .env
# nano .env

# Your domain name (must have DNS pointing to this server)
DOMAIN=kat.example.com

# Email for Let's Encrypt certificate notifications
EMAIL=admin@example.com

# CouchDB admin password (for server management only - never exposed externally)
# Minimum 12 characters recommended
COUCH_PASSWORD=change_me_to_a_very_strong_password

# Sync user password (used internally by Nginx to proxy API requests)
# This user has limited access to only the kat_sessions database
SYNC_PASSWORD=change_me_to_another_strong_password

# Fauxton admin UI password (HTTP Basic Auth for web admin access)
# Separate from CouchDB password for additional security
ADMIN_UI_PASSWORD=change_me_for_web_ui_access
